---
import Article from "../components/Article.astro";
import Comments from "../components/Comments.astro";
import LayoutTemplate from "./LayoutTemplate.astro";

const { title, pubDate, updatedDate, heroImage, category, backgroundColor, headings } = Astro.props;
---
<LayoutTemplate {title}>
    <Article {title} {pubDate} {updatedDate} {heroImage} {category} {backgroundColor} {headings}>
        <slot/>
    </Article>
    <Comments/>
</LayoutTemplate>